# 技術リファレンス: {機能名}

> このドキュメントは implementation-plan で使用されるすべての技術を、初学者向けに解説したものです。
> 言語・ライブラリ・概念に不慣れな方が implementation-plan を読む際のコンパニオンとしてお使いください。

## Technology Stack Overview

```
{この機能で使う技術同士の関係をASCII図で俯瞰する}

例:
┌─────────────┐     ┌──────────────┐
│ TypeScript  │────▶│    React     │
│  (言語)     │     │ (フレームワーク) │
└─────────────┘     └──────┬───────┘
                           │ 使用
                    ┌──────┴───────┐
                    │   zustand    │
                    │  (状態管理)   │
                    └──────┬───────┘
                           │ 保存先
                    ┌──────┴───────┐
                    │ chrome.storage│
                    │   (API)      │
                    └──────────────┘
```

---

## Language

### {言語名}

- **何か**: {1-2文の平易な説明。前提知識ゼロで理解できるように}
- **なぜ使うか**: {この機能の implementation-plan でどう使われるか}

#### 仕組みの概念図

```
{この言語の基本的な動作モデルをASCII図で示す}

例:
ソースコード (.ts)
    │
    ▼ コンパイル
JavaScript (.js)
    │
    ▼ 実行
ブラウザ / Node.js
```

#### 学習ステップ

1. **まず知るべきこと**: {この機能を理解するための最低限の知識}
   - {コンセプト1}: {1文の説明}
   - {コンセプト2}: {1文の説明}
2. **次に知るべきこと**: {実装計画を読み解くために必要な知識}
   - {コンセプト3}: {1文の説明}
   - {コンセプト4}: {1文の説明}
3. **実践で使う**: {implementation-plan の変更案で実際に使うパターン}
   - {コンセプト5}: {1文の説明}

#### コード例 — before / after

この技術を使わない場合:
```{lang}
{この技術なしで同じことをする場合のコード (3-5行)}
```

この技術を使う場合:
```{lang}
{この技術を使った場合のコード (5-10行)}
```

---

## Frameworks

### {フレームワーク名}

- **何か**: {1-2文の平易な説明}
- **なぜ使うか**: {この機能でどう使うか}

#### 仕組みの概念図

```
{このフレームワークの基本的な動作モデルをASCII図で示す}
```

#### 学習ステップ

1. **まず知るべきこと**: {最低限の知識}
   - {コンセプト}: {説明}
2. **次に知るべきこと**: {実装理解に必要な知識}
   - {コンセプト}: {説明}
3. **実践で使う**: {implementation-plan で使うパターン}
   - {コンセプト}: {説明}

#### コード例 — before / after

この技術を使わない場合:
```{lang}
{before}
```

この技術を使う場合:
```{lang}
{after}
```

---

## Libraries

### {ライブラリ名}

- **何か**: {1-2文の平易な説明}
- **なぜ使うか**: {この機能でどう使うか}

#### 仕組みの概念図

```
{このライブラリの基本的な動作モデルをASCII図で示す}
```

#### 学習ステップ

1. **まず知るべきこと**: {最低限の知識}
   - {コンセプト}: {説明}
2. **次に知るべきこと**: {実装理解に必要な知識}
   - {コンセプト}: {説明}
3. **実践で使う**: {implementation-plan で使うパターン}
   - {コンセプト}: {説明}

#### コード例 — before / after

この技術を使わない場合:
```{lang}
{before}
```

この技術を使う場合:
```{lang}
{after}
```

---

## Tools & Infrastructure

### {ツール名}

- **何か**: {1-2文の平易な説明}
- **なぜ使うか**: {この機能でどう使うか}
- **主なコマンド**:
  - `{コマンド}` — {何をするか}
  - `{コマンド}` — {何をするか}

---

## Key Concepts

### {概念名}

- **何か**: {1-2文の平易な説明}
- **なぜ重要か**: {この機能の実装でどう関係するか}

#### 仕組みの概念図

```
{この概念の動作をASCII図で示す}

例 (MutationObserver):
DOM変更
  │
  ▼ 検知
MutationObserver
  │
  ▼ コールバック
処理実行
  │
  ├─ 要素の追加検知 → ボタン注入
  └─ 要素の削除検知 → クリーンアップ
```

#### 学習ステップ

1. **まず知るべきこと**: {最低限の知識}
   - {コンセプト}: {説明}
2. **次に知るべきこと**: {実装理解に必要な知識}
   - {コンセプト}: {説明}
3. **実践で使う**: {implementation-plan で使うパターン}
   - {コンセプト}: {説明}

---

## Glossary

| 用語 | 意味 |
|------|------|
| {用語} | {平易な定義} |
