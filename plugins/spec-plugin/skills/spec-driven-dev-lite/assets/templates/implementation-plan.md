# {目的を簡潔に記述}

**関連Issue**: #{Issue番号} <!-- Issueから作成した場合のみ記載 -->

{背景・なぜこの変更が必要か、1-2文}

## システム図

### 状態マシン / フロー図

```
{状態遷移やフローをASCII罫線図で記述}
{すべてのパス・分岐・エッジケースを可視化}
```

### データフロー

```
{コンポーネント間のデータの流れをASCII罫線図で記述}
```

## 変更案

### {カテゴリ名}

#### [NEW] `{ファイルパス}`

{このファイルで何をするか}

```{lang}
{型定義・関数シグネチャ・import文}
```

#### [MODIFY] `{ファイルパス}`

{変更内容}

```{lang}
// before:
{変更前}
```

```{lang}
// after:
{変更後}
```

#### [DELETE] `{ファイルパス}`

{削除理由}

## 検証計画

**テスト方針**: {TDD / テスト追加 / 手動検証のみ}

### 自動テスト

| カテゴリ | テストケース | ユースケース / 想定シナリオ | 期待結果 |
|----------|------------|--------------------------|---------|
| 正常系 | {テストケース1} | {想定シナリオ} | {期待結果} |
| 異常系 | {テストケース2} | {想定シナリオ} | {期待結果} |

**テスト実行コマンド**: `{コマンド}`

### 手動検証

1. {確認手順1}
2. {確認手順2}

## Definition of Done

- [ ] すべてのタスク（tasks.md）が完了
- [ ] {機能固有の受入条件1}
- [ ] {機能固有の受入条件2}
- [ ] 既存機能にリグレッションがない
