# {NN}: {マイクロタスク名}

> このファイルだけで完結するタスク。他の計画ファイルを読む必要はない。
> 書かれていないファイルには触れないこと。

## ゴール

{このタスクで達成することを1〜2文。例: `src/content/ranking.ts` にユーザーID抽出関数を追加する}

## 前提

- 依存: {先に完了している必要があるタスク。例: `01-types.md`（型定義）/ なし}
- 対象ファイル: `{ファイルパス}`（{NEW / MODIFY}）

## 既存コードの抜粋

{このタスクの実装に必要な既存コード。シグネチャ・該当ブロックのみ。NEW で参照不要なら「なし」}

```{lang}
// {file:line}
{抜粋}
```

## 実装内容

{implementation-plan から転記したコード骨格。要約・省略禁止}

```{lang}
{コード}
```

{コードだけで伝わらない補足があれば1〜3行で。例: エラー時は null を返す（throw しない）}

## このタスクでやらないこと

- {隣接タスクに含まれる作業。例: manifest.json への登録は 04 で行う}
- {触ってはいけないファイル・既存関数}

## 完了条件

- [ ] {実行可能な検証。例: `npx tsc --noEmit` がエラーなしで通る}
- [ ] {テスト・目視手順があれば}
